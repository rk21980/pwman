<!DOCTYPE html>
<html>
<head>
    <script type="text/javascript" src="js/core.js"></script>
</head>
<body>
<section>
    <?php
        if(property_exists($this, "secret")): ?>
            <form method="post" action="">
                <input type="text" name="login" placeholder="name" />
                <input type="password" name="login" placeholder="password" />
                <input type="hidden" name="secret" value="<?=$this->secret?>" />
            </form>
        <?php
        elseif(property_exists($this, "qrcode")):
            echo $this->qrcode;
        else:
        ?>
            <p>I got nothing on this</p>
        <?php
        endif;
    ?>
</section>
<p>Names of my friends:</p>
<ul>
    <?php foreach ($this->friends as $friend): ?>
        <li><?=$friend?></li>
    <?php endforeach; ?>
</ul>
</body>
</html>